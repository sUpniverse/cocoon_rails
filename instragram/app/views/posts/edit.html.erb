<div class="container">
	<!-- <form action="/posts/<%=@post.id%>" method="POST" enctype="multipart/form-data">		
		<input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
		<input type="hidden" name="_method" value="PUT">
		<table class="table">
			<thead>
				<th><%=@post.id%>번 글수정</th>
			</thead>
			<tbody>
		        <tr>
		          <td>
		            <input class="form-control" type="text" name="post[title]" placeholder="제목" value="<%=@post.title%>">
		          </td>
		        </tr>
		        <tr>
		          <td>
		            <input class="form-control" type="text" name="post[contents]"  placeholder="내용" value="<%=@post.contents%>">        
		          </td>
		        </tr>
		        <tr>
           			<td><input type="file" name="post[Avatar]"></td>
          		</tr>
	      </tbody>
		</table>
		<input type="submit" value="수정하기" class="btn btn-primary">
	</form> -->
	
	<%= form_tag("/posts/#{@post.id}", method:"PUT", multipart: true ,class: "form-group")do %>
		<table class="table">
			<thead>
				<th><%=@post.id%>번 글수정</th>
			</thead>
			<tbody>
		        <tr>
		          <td>
		          	<%= label_tag(:post_title ,"제목") %>
					<%= text_field_tag("post[title]", @post.title, class: "form-control") %> 
		          </td>
		        </tr>
		        <tr>
		          <td>
		          	<%= label_tag(:post_title ,"내용") %>
					<%= text_field_tag("post[contents]", @post.contents, class: "form-control") %>		                    
		          </td>
		        </tr>
		        <tr>
           			<td>
           				<%= file_field_tag("post[Avatar", class: "btn btn-default") %> 
           			</td>
          		</tr>
	      </tbody>
		</table>		
		<%= button_tag("전송",class: "btn btn-primary") %>
	<% end%>
</div>